% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hinge_wtsvr.R
\name{hinge_wtsvr}
\alias{hinge_wtsvr}
\title{Hinge Weighted Twin Support Vector Regression}
\usage{
hinge_wtsvr(
  X,
  y,
  C1 = 1,
  C2 = C1,
  epsilon1 = 0.1,
  epsilon2 = epsilon1,
  D1 = rep(1, nrow(X)),
  D2 = rep(1, nrow(X)),
  kernel = c("linear", "rbf", "poly"),
  gamma = 1/ncol(X),
  degree = 3,
  coef0 = 0,
  eps = 1e-07,
  max.steps = 4000,
  fit_intercept = TRUE,
  reduce_set = NULL,
  weight_f1 = NULL,
  weight_f2 = NULL,
  weight_option1 = NULL,
  weight_option2 = NULL
)
}
\arguments{
\item{X, y}{dataset and label.}

\item{C1, C2}{plenty term.}

\item{epsilon1, epsilon2}{parameter for epsilon tube.}

\item{D1, D2}{weight vectors for least squares loss and hinge loss, respectively, applied per sample.}

\item{kernel}{kernel function. The definitions of various kernel functions are as follows:
\describe{
    \item{linear:}{\eqn{u'v}{u'*v}}
    \item{poly:}{\eqn{(\gamma u'v + coef0)^{degree}}{(gamma*u'*v + coef0)^degree}}
    \item{rbf:}{\eqn{e^{(-\gamma |u-v|^2)}}{exp(-gamma*|u-v|^2)}}
}}

\item{gamma}{parameter for \code{'rbf'} and \code{'poly'} kernel. Default \code{gamma = 1/ncol(X)}.}

\item{degree}{parameter for polynomial kernel, default: \code{degree = 3}.}

\item{coef0}{parameter for polynomial kernel,  default: \code{coef0 = 0}.}

\item{eps}{the precision of the optimization algorithm.}

\item{max.steps}{the number of iterations to solve the optimization problem.}

\item{fit_intercept}{if set \code{fit_intercept = TRUE},
the function will evaluates intercept.}

\item{reduce_set}{reduce set for reduce SVM, default \code{reduce_set = NULL}.}

\item{weight_f1, weight_f2}{optional functions to compute sample weights for the
least squares loss and hinge loss, respectively. Default is NULL, meaning the
corresponding \code{D1} or \code{D2} vector is used directly. When a function is
supplied and return a numeric vector of length \code{nrow(X)}. If provided, the relevant \code{D1} or
\code{D2} argument is ignored.}

\item{weight_option1, weight_option2}{optional named lists of additional arguments
to be passed to \code{weight_f1} and \code{weight_f2}, respectively. Default is
NULL (no extra arguments). These are passed via \code{do.call}, for example
\code{weight_option1 = list(k = 2)} if \code{weight_f1} expects a \code{k}
parameter.}
}
\value{
return \code{TSVMRegressor} object.
}
\description{
\code{hinge_wtsvr} is an R implementation of Hinge Weighted Twin Support Vector Regression
}
\author{
Zhang Jiaqi.
}
